// class {}    // Error at '{': Expect class name.

class Brioche {}
print Brioche;      // prints Brioche
print Brioche();    // prints Brioche instance

var obj = "not an instance";
// print obj.field;    // "Only instances have properties."

class Pair {}

var pair = Pair();
pair.first = 1;
pair.second = 2;
print pair.first + pair.second; // 3

class Brunch {
    bacon() {}
    eggs() {}
}

var brunch = Brunch();
var eggs = brunch.eggs;
eggs();

class Scone {
    filling() {}
    topping(first, second) {
        print "scone with " + first + " and " + second;
    }
}

var scone = Scone();
scone.topping("berries", "cream");
var sc = scone.topping;
sc("berries", "cream");

